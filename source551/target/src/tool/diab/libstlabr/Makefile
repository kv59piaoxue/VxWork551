# Makefile - makefile for extracting objects from libstlabr.a
#
# modification history
# --------------------
# 01a,29jan03,sn   wrote
#
# DESCRIPTION
# This file contains the definitions needed for extracting objects
# from libstlabr.a (Abridged C++ Standard Library). In releases
# prior to to Diab 5.1 there is no abridged library so
# we use the full library instead.

TGT_DIR          = $(WIND_BASE)/target

CXX_STL_VARIANT  = abridged
DIABLIBRARY      = libstlabr.a libstl.a

include $(TGT_DIR)/src/tool/diab/libstlstd/defs.stl
